import React from "react";
import { Text, View } from "react-native";
import { ListMovie } from "../reactnative/listMovie";


export const Ecran3=({navigation, route})=>{
    return(
        <View>
            <ListMovie/>
        </View>
    )
}